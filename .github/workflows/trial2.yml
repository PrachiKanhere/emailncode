
on: push

jobs:
  job1:
    runs-on: windows-latest
    # Map a step output to a job output
    outputs:
      output1: ${{ steps.step1.outputs.test}}
    steps:
      - name: checkout
        uses: actions/checkout@v2.4.2
        
      - id: step1
        run: echo "::set-output name=test::Following Buses might be down:' '[intjmsbus,jmsbus, ysbus]"
     
  Groovy-Script:
    runs-on: windows-latest
    needs: job1
    steps:
      - name: checkout
        uses: actions/checkout@v2.4.2
     
      - name : take file
        run: | 
          echo ${{needs.job1.outputs.output1}}
          echo ${{needs.job1.outputs.output1}} > abc1.txt
           
      - name: setup Python
        uses: actions/setup-python@v3
        
      - name: install dependencies
        run : python -m pip install pytz
        
      - name: run py script
        run: |
          python .github/workflows/Timestamp.py > output
          echo "::set-output name=TIMESTAMP::$output"
        id: step1
        
           
      - name: run py script
        run: |
          python .github/workflows/date.py > output
          echo "::set-output name=formatteddate::$output"
        id: step2
        
        
      - name: run py script
        run: |
          python .github/workflows/down_bus_check.py > output
          echo "::set-output name=DOWN_BUS::$output"
          echo "$output"
          echo "output"
          echo %output%
          echo "%output%"
        id: step3
        
          
      - name: Print op
        run: |
          echo "${{steps.step1.outputs.TIMESTAMP}}"
          echo "${{steps.step2.outputs.formatteddate}}"
          echo "${{steps.step3.outputs.DOWN_BUS}}"
  

    
